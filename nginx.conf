server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    # Suporte para crawlers de redes sociais (Facebook, WhatsApp, etc)
    # Garante que eles sempre acessem o index.html para ler as meta tags OG
    if ($http_user_agent ~* "facebookexternalhit|WhatsApp|Facebot|Twitterbot") {
        rewrite ^ /index.html break;
    }

    # Configurações de Cache para assets estáticos
    location /assets/ {
        root /usr/share/nginx/html;
        expires 1y;
        add_header Cache-Control "public, no-transform";
    }

    # Gzip para melhor performance
    gzip on;
    gzip_vary on;
    gzip_min_length 10240;
    gzip_proxied expired no-cache no-store private auth;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml;
    gzip_disable "MSIE [1-6]\.";
}